google-api-python-client==2.111.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0
python-dotenv==1.0.0
pytz==2024.1
# Streamlitはバージョン固定（未固定だとクラウドが最新版を入れ、components.htmlの
# iframe挙動変更で左上アイコン注入JS等が動かなくなる。デスクトップと同じ版に揃える）
streamlit==1.37.1
plotly>=5.24.0
pandas>=2.0.0
anthropic>=0.40.0
scikit-learn>=1.3.0
numpy>=1.24.0
# PyTorch（深層学習で拡張する場合・任意）: pip install torch

streamlit-sortables==0.3.1
streamlit-calendar==1.4.0

# クラウド常時公開（Turso/libSQL = クラウド版SQLite）。純Pythonでビルド不要。
libsql-client>=0.3.1
